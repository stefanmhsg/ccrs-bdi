# ============================================================================
# CCRS LLM Configuration Template
# ============================================================================
# Copy this file to .env and fill in your actual values.
# The .env file will be loaded automatically at application startup.
#
# Required for LLM-based contingency strategies:
#   - PredictionLlmStrategy (Level 2): Autonomous prediction using LLM
#   - ConsultationStrategy (Level 4): Human-in-the-loop with LLM assistance
#
# If API key is not provided, these strategies will be disabled gracefully.
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI API Configuration (Primary)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-...your-key-here...

# Optional: Custom OpenAI base URL (for proxies or compatible APIs)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Override default model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_MODEL=gpt-4o
# OPENAI_MODEL=gpt-3.5-turbo

# Optional: Organization ID (if using organizational API keys)
# OPENAI_ORGANIZATION_ID=org-...

# ----------------------------------------------------------------------------
# Generic LLM Configuration (Fallback)
# ----------------------------------------------------------------------------
# These are used if OPENAI_* variables are not set.
# Useful for non-OpenAI providers (e.g., Anthropic, local models).

# LLM_API_KEY=your-api-key-here
# LLM_BASE_URL=https://your-llm-provider.com/v1
# LLM_MODEL=your-model-name

# ============================================================================
# Usage Notes
# ============================================================================
#
# 1. Variable Priority:
#    - OPENAI_* variables are checked first
#    - LLM_* variables are used as fallback
#    - Built-in defaults apply if nothing is set
#
# 2. Supported Providers:
#    - OpenAI (gpt-4o-mini, gpt-4o, gpt-3.5-turbo)
#    - OpenAI-compatible APIs (set OPENAI_BASE_URL)
#    - Azure OpenAI (set OPENAI_BASE_URL to Azure endpoint)
#    - Local models via OpenAI-compatible servers
#
# 3. Environment Variable Loading:
#    - Loaded in CcrsAgentArch.init() before JaCaMo starts
#    - Variables injected into system properties
#    - Langchain4jLlmClient.fromEnvironment() reads them automatically
#
# 4. Security:
#    - NEVER commit .env file to version control
#    - .env is in .gitignore by default
#    - Share .env.example (this file) with team members
#
# ============================================================================
